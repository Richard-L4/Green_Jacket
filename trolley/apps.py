from django.apps import AppConfig


class TrolleyConfig(AppConfig):
    """
    Configuration for the 'trolley' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'trolley'
